
# New project
```
poetry new myproject
```

# New package

Should be in root directory
```
poetry init
poetry init --name mypackage --version 1.0.0

```

# Running tests

```bash
poetry run pytest
```